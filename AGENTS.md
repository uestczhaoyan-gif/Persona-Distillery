# 人物视角项目指令

## 人物快捷入口

当用户消息以项目快捷入口开头时，按 `直接对话/命令入口.md` 解析人物并进入直接对话：

- `/孔子`、`/鲁迅`、`/费曼`、`/庄子`
- `/persona <person-id>`，例如 `/persona kongzi`
- `人物 孔子`、`进入孔子人物视角`、`我想和孔子交流`
- `/人物` 查看清单，`/退出` 或“退出人物视角”恢复普通助手。

解析使用 `直接对话/persona-aliases.json` 的精确别名或现有稳定人物 ID，不猜测名称。先读取 `直接对话/命令入口.md`，按其流程调用 `直接对话/scripts/resolve_persona.py`，再真正读取加载计划中的文件。完整回答契约只维护在 `直接对话/prompts/agent.md`。

`draft/private` 预览需要已有用户授权，授权可来自当前会话或 `.persona/preview.local.json`。本地清单只适用于列出的历史公共人物和只读预览，不扩展到新人物、原文、私密增强层或公开发布；既有授权不要重复询问。

快捷入口不是 Codex 内置命令；`/persona` 也可能被客户端拦截，遇到此情况改用 `人物 孔子`。代码块、引号和讨论启动方式的消息不触发人物模式。项目整理、开发、审查等请求保持普通工程助手模式。

## 项目工作方式

- 先读取相关模块的 `README.md` 和人物 `manifest.json`，再修改内容。
- 人物事实、证据和来源必须保持可回链；不要把模拟内容写成原话。
- 默认只读人物包和会话；只有用户明确要求时才写入日志或研究文件。
- 项目文档放 `docs/`；新功能的协议、脚本、Schema 和测试放对应模块或 `tests/`。人物蒸馏相关契约与脚本继续放 `人物蒸馏/schemas/` 和 `人物蒸馏/scripts/`。详见 `docs/03-项目结构与贡献指南.md`。
- 不提交原始受版权保护材料、私密上传、API 密钥、完整私聊或生成会话。
