# 原始资料状态：理查德·费曼

当前没有将远程原文批量保存到本目录。此人物包采用公开来源登记与人工阅读提炼；论文、课程、访谈和图书多受各自版权限制，因此只在 `01-sources.csv` 保存地址、版本与定位，在 `02-evidence.md` 保存原创证据概括。

`.gitkeep` 不是资料文件，只用于保留空目录。将来取得许可或用户合法上传原文后，可把文件放在本地 `raw/`，计算校验值并通过增量流水线生成新的片段；默认不提交公开仓库。

